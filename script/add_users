#!/usr/bin/env ruby
# encoding: utf-8

require File.expand_path("../../config/environment",  __FILE__)

users = ["Tiësto", "Passion Pit"]

$redis.del("users")
users.each do |user|
  $redis.lpush("users", user)
end